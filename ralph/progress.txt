# Ralph Progress Log
Started: Thu Jun 11 14:16:16     2026
---

## Codebase Patterns
- Notebook metadata stored in `notebooks_meta.json` via `_load_meta()` / `_save_meta()` helpers in `backend/app/routers/notebooks.py`
- Use `from __future__ import annotations` in schemas when adding forward-referencing types
- Always use `.get("key", default)` when reading optional fields from JSON metadata to remain backward-compatible with older records
- Frontend TypeScript types mirror backend Pydantic schemas in `frontend/src/types/index.ts`
- Python import test: `python -c "from app.models.schemas import NotebookResponse, ChatMessage"` from the `backend/` directory
- TypeScript check: `npx tsc --noEmit` from the `frontend/` directory (with `npm install` first if node_modules missing)

## 2026-06-11 14:20 UTC - US-001
- Added `tags: list[str]` to NotebookCreate, NotebookUpdate, NotebookResponse in `backend/app/models/schemas.py`
- Added `title: Optional[str]` and `pinned: bool` to ChatRequest in schemas.py
- Created `ChatMessage` Pydantic model with id, role, content, citations, timestamp, title, pinned
- Updated notebook create/update/get/list endpoints to persist and return tags
- Updated frontend `Notebook` interface with `tags: string[]` and `ChatMessage` with `title?/pinned?`
- Files changed: `backend/app/models/schemas.py`, `backend/app/routers/notebooks.py`, `frontend/src/types/index.ts`
- **Learnings for future iterations:**
  - The notebook metadata is stored in a plain JSON file (`notebooks_meta.json`), not in a database. Use `.get()` with defaults when reading to maintain backward compat.
  - `from __future__ import annotations` needed for forward references like `ChatMessage.citations: list[ChatCitation]`
  - Python's `python` command is at the system path but needs `sys.path.insert(0, '.')` or running from `backend/` directory
  - Frontend needs `npm install` before first `tsc` run — node_modules not committed to repo
---

## 2026-06-11 14:45 UTC - US-003
- Modified `list_notebooks()` to accept optional `?tag=xxx` query param and filter results
- Added `GET /api/tags` endpoint returning deduplicated sorted list of all unique tags
- Added `DELETE /api/tags/{name}` endpoint removing tag from all notebooks with 400 on empty name
- Files changed: `backend/app/routers/notebooks.py`
- **Learnings for future iterations:**
  - Tags are stored in `notebooks_meta.json` alongside notebook metadata — no separate storage
  - Use `_load_meta()` / `_save_meta()` helpers for all notebook metadata operations
  - Tag filtering uses exact match (`tag in nb_tags`), not substring or case-insensitive
  - The `DELETE /api/tags/{name}` endpoint is idempotent — returns success even if tag not found on any notebook
  - Route order matters in FastAPI: `/api/tags` (static) must not conflict with `/api/notebooks/{id}` (parameterized)
  - Python import check requires full path to python.exe when multiple Python installations exist on Windows
- Use `py` (Python launcher) instead of `python` on Windows — `python` may be a Windows App execution alias that fails with exit code 49
- ChromaDB global_search collection stores notebook_id and notebook_name in chunk metadata alongside document-level metadata (source, filename, doc_id)
- New routers need three changes: create router file, import in main.py, add app.include_router()
---

## 2026-06-11 ~15:00 UTC - US-004
- Added `SearchResult` Pydantic model with snippet, source, notebook_id, notebook_name in schemas.py
- Added `query_global()` function to database.py following the existing `query_chunks()` pattern
- Created `backend/app/routers/search.py` with `GET /api/search?q=xxx` endpoint
- Registered search router in main.py
- Endpoint: embeds query → queries global_search ChromaDB collection → deduplicates by snippet → returns SearchResult list
- Returns empty array when query is empty or no results found
- Files changed: `backend/app/models/schemas.py`, `backend/app/database.py`, `backend/app/routers/search.py` (new), `backend/app/main.py`
- **Learnings for future iterations:**
  - The `global_search` collection was already being populated (via `add_to_global_index()` in document upload/URL fetch) and cleaned up (via `remove_from_global_index()` on document delete) but had no query endpoint until now
  - Chunk metadata in global_search includes both document-level fields (source, filename, doc_id) and notebook-level fields (notebook_id, notebook_name) injected by `add_to_global_index()`
  - When querying ChromaDB collections, always check `col.count() == 0` first to avoid errors on empty collections
  - Use `py` instead of `python` for Python commands on Windows — see Codebase Patterns
---

## 2026-06-11 ~15:15 UTC - US-005
- Added `ConversationSyncRequest` Pydantic model in schemas.py (notebook_id + list[ChatMessage])
- Added conversation storage helpers `_load_conversations()` / `_save_conversations()` in chat.py, storing to `data/conversations/{notebook_id}.json`
- Added `POST /api/chat/sync` endpoint to persist conversation messages from frontend
- Added `GET /api/chat/export/{notebook_id}` endpoint returning Markdown with notebook name, export date, role-labelled messages with citations as footnotes, Content-Disposition: attachment
- Added notebook existence validation in sync endpoint (404 if notebook not found)
- Added conversation file cleanup in `delete_notebook()` in notebooks.py
- Files changed: `backend/app/models/schemas.py`, `backend/app/routers/chat.py`, `backend/app/routers/notebooks.py`
- **Learnings for future iterations:**
  - Conversations are stored in `data/conversations/{notebook_id}.json` — same file-per-notebook pattern as notes
  - Export Markdown uses `[^n]` footnote syntax (both body reference and definition must have `^` for proper linking)
  - Avoid importing private helpers across router modules — inline the lookup or use a shared utility
  - Notebook name lookup reads `notebooks_meta.json` directly instead of importing from notebooks router
  - `PlainTextResponse` with `text/markdown` media type and `Content-Disposition: attachment` triggers download
---

## 2026-06-11 ~15:30 UTC - US-006
- Added `GET /api/notebooks/{notebook_id}/export` endpoint returning a ZIP file via StreamingResponse
- ZIP contains: README.md (overview table), conversation.md, metadata.json, and documents/ folder with original files
- Built entirely in memory using `io.BytesIO` + `zipfile.ZipFile` — no temp files on disk
- Added `_build_export_conversation_md()` helper (same pattern as chat.py US-005 export, reused locally)
- Added `_build_export_readme()` helper generating a markdown overview table with counts and tags
- ZIP entries wrapped in a `{safe_name}/` prefix folder, using `zipfile.ZIP_DEFLATED` compression
- Returns 404 if notebook not found, proper `Content-Disposition: attachment` header
- Files changed: `backend/app/routers/notebooks.py`
- **Learnings for future iterations:**
  - StreamingResponse with BytesIO generator works for in-memory ZIP delivery — wrote to BytesIO, seek(0), then yielded chunks
  - The `_build_export_conversation_md()` helper duplicates chat.py's export pattern; could be extracted to a shared utility if needed by more endpoints
  - PowerShell jq has issues with hyphens in string literals on Windows — use PowerShell's ConvertFrom-Json/ConvertTo-Json for PRD updates instead
  - This story is purely backend — no frontend changes (US-013 will add the UI button)
---

## 2026-06-11 ~16:00 UTC - US-007
- Added `SuggestedQuestionsRequest` and `SuggestedQuestionsResponse` Pydantic models in schemas.py
- Added `POST /api/chat/suggested-questions` endpoint with count validation (default 4, range 1-8)
- Added `_generate_suggested_questions()` async helper with layered fallback strategy:
  - Uses generic probe embedding ("key concepts main ideas overview...") to find representative chunks
  - Falls back to `GENERIC_QUESTIONS` (8 hardcoded questions) when: no documents, no embeddings, no LLM configured, or LLM API failure
  - LLM prompt generates exactly `count` questions from document context, strips numbering/bullets, pads with generics if short
- Endpoint returns 404 for non-existent notebooks via existing `_notebook_exists()` helper
- Files changed: `backend/app/models/schemas.py`, `backend/app/routers/chat.py`
- **Learnings for future iterations:**
  - The `_generate_suggested_questions` helper uses lazy imports (`from app.services.embedder import embed_query`) inside the function body to avoid circular imports — a pattern already used elsewhere in the codebase
  - The codebase consistently declares `async def` even for functions that only contain blocking calls (OpenAI, ChromaDB) — follow this convention for consistency
  - `Count` parameter validation uses Pydantic's `Field(ge=1, le=8)` which handles 400 errors automatically before the handler runs
  - For PRD updates, use PowerShell's `ConvertFrom-Json`/`ConvertTo-Json` cycle; avoid `jq` on Windows due to hyphen-in-literal issues
---
